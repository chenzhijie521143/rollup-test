{
  "name": "rollup-test",
  "version": "1.0.0",
  "description": "rollup 学习",
  "main": "index.js",
  "scripts": {
    "dev": "npm run start",
    "start": "rm -rf example/dist && yarn && rollup -wc ./build/rollup.dev.js",
    "build": "rm -rf dist && yarn && rollup -c ./build/rollup.build.js"
  },
  "author": "chenzhijie",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^2.74.1",
    "rollup-plugin-livereload": "^2.0.5",
    "rollup-plugin-serve": "^1.1.0"
  }
}
